from math import *

pi = atan(1) * 4
print(cos(0 * pi / 180))

for (int i = 1; i <= n; i++) {
    int n, m;
    std::cin >> n >> m;
    for (int j = 1; j <= m; j++) {
        int n, m;
        std::cin >> n >> m;
    }
}