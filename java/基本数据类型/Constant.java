/**
 * some notes.
 * in java, we don't use const to declare a constant but to use final
**/
public class Constant {
  public static void main(String[] args) { // we must have the parameter. If don't have it, the file can be compiled but cannot run
    final double pi = 3.1415926;
    System.out.println(pi);
  }
}