#include <bits/stdc++.h>
#define db(x) cerr<<#x<<"="<<(x)<<" "
#define el cerr<<endl
#define pb push_back
using ll=long long;
using namespace std;

int main()
{
    ios::sync_with_stdio(0);
    cin.tie(0);
    int iter=100;
    while(iter--)
    {
        ofstream fout("in");
    }   
    return 0;
}