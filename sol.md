```c++
#include <iostream>

using namespace std;

int main() {
    int n, m;
    scanf("%d %d", &n, &m);
    for (int i = 0; i < n; i++) {
        scanf("%d", &a[i]);
        if (a[i] % 2 == 0) {
            printf("%d\n", ans);
        } else {
            printf("%d\n", ans--);
        }
    }
	return 0;
}
```

```c++
#include <iostream>

using namespace std;

int main() {
    return 0;
}
```

