#include <bits/stdc++.h>
using namespace std;
#define debug(x) cerr << #x << " = " << x << endl
int main() {
    ios::sync_with_stdio(0);
    cin.tie(0);
    double n;
    cin >> n;
    cout << fixed << setprecision(2) << n << '\n';   
    return 0;
}