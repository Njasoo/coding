#include <bits/stdc++.h>
using namespace std;
#define debug(x) cerr << #x << " = " << x << endl
int main() {
    ios::sync_with_stdio(0);
    cin.tie(0);
    int n;
    char ch;
    cin >> n >> ch;
    cout << ++n << '\n' << ch << '\n';   
    return 0;
}