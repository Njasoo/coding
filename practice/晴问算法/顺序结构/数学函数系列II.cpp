#include <bits/stdc++.h>
using namespace std;
#define debug(x) cerr << #x << " = " << x << endl
int main() {
    ios::sync_with_stdio(0);
    cin.tie(0);
    double d;
    cin >> d;
    cout << fixed << setprecision(2) << sqrt(d) << " ";
    cout << fixed << setprecision(2) << log(d) << '\n';   
    return 0;
}