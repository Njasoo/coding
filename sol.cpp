#include <bits/stdc++.h>
using namespace std;
int main() {
  int n, m;
  scanf("%d %d", &n, &m);
  printf("%d %d\n", &n, &m);
  for (int i = 0; i < len; i++) {
    scanf("%d", a[i]);
  }
  if (a[i] % 2 == 0) {
    ans++;
  }
  printf("%lld\n", ans);
  return 0;
}