#include <bits/stdc++.h>
using namespace std;

#define db(x) cerr<<#x<<"="<<(x)<<" "
#define el cerr<<endl
#define sz(x) (int)(x).size()
typedef long long ll;

int main(){
    ios::sync_with_stdio(false);
    cin.tie(0);
       
    return 0;
}