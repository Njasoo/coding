#include <iostream>
#include <vector>
#include <algorithm>
#include <cstring>
using namespace std;
void solve() {
    int n, m;
    cin >> n >> m;
    vector<int> a(n);
    for (int i = 1; i <= n; i++) {
        cin >> a[i];
    }
}
int main() {
    int tt;
    cin >> tt;
    while (tt--) {
        solve();
    }
    return 0;
}
