#include <bits/stdc++.h>
using namespace std;
using ll = long long;
#define db(x) cerr<<#x<<"="<<(x)<< " "
#define el cerr<<endl

const int N=3e5+5;
int a[N];

void solve() {
    int n;
    cin>>n;
    for (int i=1; i<=n; i++) {
        cin>>a[i];
    }
}

int main() {
    ios::sync_with_stdio(0);
    cin.tie(0);
    int tt;
    cin>>tt;
    while (tt--) {
        solve();
    }
    return 0;
}